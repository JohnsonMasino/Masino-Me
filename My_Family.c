#include <stdio.h>

/**
 * main - prints my family members ages
 * Return: Always 0 (Success)
 * Code by Masino
 */

int main(void)
{
	unsigned int a;
	
	for (a > 0; a < 10; a++)
	{
		if (a = 1)
		{
			printf("The age of %d is: 23\n", a);
		}
		if (a = 2)
		{
			printf("The age of %d is: 19\n", a);
		}
		if (a = 3)
		{
			printf("The age of %d is: 17\n", a);
		}
		if (a = 4)
		{
			printf("The age of %d is: 15\n", a);
		}
		if (a = 5)
		{
			printf("The age of %d is: 12\n", a);
		}
		else
		{
			printf("ERROR: Data for %d not found\n", a);
		}
	}
	else
	{
		printf("Error here\n");
	}
	return (0);
}
