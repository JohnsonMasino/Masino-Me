#include <stdio.h>

/**
 * main - addresses of a variable
 *
 * Return: Always 0.
 * Code by Masino
 */

int main(void)
{
	char c;
	int n;

	printf("Address of variable 'c': %p\n", &c);
	printf("Address of variable 'n': %p\n", &c);
	return (0);
}
